# 目标检测系统开发文档

## 1. 项目概述

### 1.1 项目名称
智能目标检测系统 (Intelligent Object Detection System)

### 1.2 项目描述
基于YOLOv8算法的Web端目标检测系统，支持图像和视频上传，自动进行目标检测，并提供结果可视化和数据管理功能。

### 1.3 技术栈
- **后端**: Python + FastAPI
- **前端**: HTML + CSS + JavaScript + Bootstrap
- **算法**: YOLOv8 (Ultralytics)
- **数据库**: MySQL
- **其他**: OpenCV, Pillow, SQLAlchemy

## 2. 系统架构

### 2.1 整体架构图
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端界面      │───▶│   FastAPI后端   │───▶│   MySQL数据库   │
│   (HTML/JS)     │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                              ▼
                       ┌─────────────────┐
                       │   YOLOv8算法    │
                       │   目标检测      │
                       └─────────────────┘
```

### 2.2 目录结构
```
object_detection_system/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI应用入口
│   ├── models/                 # 数据模型
│   │   ├── __init__.py
│   │   └── database.py
│   ├── routers/                # 路由处理
│   │   ├── __init__.py
│   │   ├── upload.py
│   │   ├── detection.py
│   │   └── results.py
│   ├── services/               # 业务逻辑
│   │   ├── __init__.py
│   │   ├── detection_service.py
│   │   └── database_service.py
│   └── utils/                  # 工具函数
│       ├── __init__.py
│       └── file_utils.py
├── static/                     # 静态文件
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   └── uploads/                # 上传文件存储
│       ├── images/
│       └── results/
├── templates/                  # HTML模板
│   ├── index.html
│   ├── upload.html
│   ├── results.html
│   └── history.html
├── weights/                    # 模型权重文件
│   └── yolov8n.pt
├── requirements.txt
├── config.py
└── README.md
```

## 3. 功能需求

### 3.1 文件上传功能
- **支持格式**: JPG, PNG, JPEG, MP4, AVI, MOV
- **文件大小限制**: 图片≤10MB, 视频≤100MB
- **上传方式**: 拖拽上传 + 点击选择
- **预览功能**: 上传前预览图片/视频

### 3.2 目标检测功能
- **检测算法**: YOLOv8 (n/s/m/l/x版本可选)
- **检测类别**: COCO 80类目标
- **置信度阈值**: 可调节 (默认0.5)
- **NMS阈值**: 可调节 (默认0.45)
- **批量检测**: 支持多文件同时检测

### 3.3 结果可视化
- **检测框绘制**: 不同类别用不同颜色
- **标签显示**: 类别名称 + 置信度
- **统计信息**: 检测到的目标数量统计
- **结果对比**: 原图与检测结果对比显示

### 3.4 数据管理
- **检测历史**: 所有检测记录存储
- **结果下载**: 检测结果图片下载
- **数据统计**: 检测数量、成功率等统计
- **数据删除**: 支持删除历史记录

## 4. 数据库设计

### 4.1 数据表结构

#### 检测记录表 (detection_records)
```sql
CREATE TABLE detection_records (
    id INT PRIMARY KEY AUTO_INCREMENT,
    filename VARCHAR(255) NOT NULL,
    original_path VARCHAR(500) NOT NULL,
    result_path VARCHAR(500) NOT NULL,
    file_type ENUM('image', 'video') NOT NULL,
    file_size BIGINT NOT NULL,
    detection_time DATETIME NOT NULL,
    processing_time FLOAT NOT NULL,
    confidence_threshold FLOAT DEFAULT 0.5,
    nms_threshold FLOAT DEFAULT 0.45,
    model_version VARCHAR(50) DEFAULT 'yolov8n',
    status ENUM('pending', 'processing', 'completed', 'failed') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 检测结果表 (detection_results)
```sql
CREATE TABLE detection_results (
    id INT PRIMARY KEY AUTO_INCREMENT,
    record_id INT NOT NULL,
    class_name VARCHAR(100) NOT NULL,
    confidence FLOAT NOT NULL,
    bbox_x1 FLOAT NOT NULL,
    bbox_y1 FLOAT NOT NULL,
    bbox_x2 FLOAT NOT NULL,
    bbox_y2 FLOAT NOT NULL,
    FOREIGN KEY (record_id) REFERENCES detection_records(id) ON DELETE CASCADE
);
```

#### 系统统计表 (system_stats)
```sql
CREATE TABLE system_stats (
    id INT PRIMARY KEY AUTO_INCREMENT,
    total_detections INT DEFAULT 0,
    total_objects_detected INT DEFAULT 0,
    avg_processing_time FLOAT DEFAULT 0.0,
    most_detected_class VARCHAR(100) DEFAULT '',
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## 5. API接口设计

### 5.1 文件上传接口
```
POST /api/upload
Content-Type: multipart/form-data

Parameters:
- file: 上传的文件
- confidence: 置信度阈值 (可选, 默认0.5)
- nms_threshold: NMS阈值 (可选, 默认0.45)
- model_version: 模型版本 (可选, 默认yolov8n)

Response:
{
    "success": true,
    "message": "文件上传成功",
    "task_id": "uuid-string",
    "filename": "uploaded_file.jpg"
}
```

### 5.2 检测状态查询接口
```
GET /api/detection/status/{task_id}

Response:
{
    "task_id": "uuid-string",
    "status": "completed",
    "progress": 100,
    "result": {
        "record_id": 123,
        "objects_count": 5,
        "processing_time": 2.34,
        "result_url": "/static/results/result_123.jpg"
    }
}
```

### 5.3 检测结果查询接口
```
GET /api/results/{record_id}

Response:
{
    "record_id": 123,
    "filename": "test.jpg",
    "detection_time": "2024-01-01 12:00:00",
    "objects": [
        {
            "class": "person",
            "confidence": 0.85,
            "bbox": [100, 200, 300, 400]
        }
    ],
    "statistics": {
        "total_objects": 5,
        "classes_detected": ["person", "car", "bicycle"]
    }
}
```

### 5.4 历史记录接口
```
GET /api/history

Parameters:
- page: 页码 (默认1)
- limit: 每页数量 (默认20)
- file_type: 文件类型过滤 (可选)

Response:
{
    "total": 100,
    "page": 1,
    "limit": 20,
    "records": [
        {
            "id": 123,
            "filename": "test.jpg",
            "detection_time": "2024-01-01 12:00:00",
            "objects_count": 5,
            "status": "completed"
        }
    ]
}
```

## 6. 前端页面设计

### 6.1 主页面 (index.html)
- **导航栏**: Logo + 功能菜单
- **上传区域**: 拖拽上传 + 参数设置
- **最近检测**: 显示最近5次检测结果
- **统计面板**: 总检测次数、成功率等

### 6.2 上传页面 (upload.html)
- **文件选择**: 支持多文件选择
- **参数配置**: 置信度、NMS阈值、模型版本
- **预览区域**: 上传文件预览
- **进度显示**: 上传和检测进度条

### 6.3 结果页面 (results.html)
- **图片对比**: 原图与检测结果对比
- **检测信息**: 检测到的目标列表
- **统计图表**: 各类别目标数量饼图
- **操作按钮**: 下载、删除、重新检测

### 6.4 历史页面 (history.html)
- **记录列表**: 分页显示历史记录
- **搜索过滤**: 按文件名、时间、状态过滤
- **批量操作**: 批量删除、批量下载
- **统计图表**: 检测趋势图

## 7. 开发计划

### 7.1 第一阶段 (第1-2周)
- [x] 项目环境搭建
- [x] 数据库设计和创建
- [x] FastAPI基础框架搭建
- [x] YOLOv8模型集成测试

### 7.2 第二阶段 (第3-4周)
- [ ] 文件上传功能开发
- [ ] 目标检测服务开发
- [ ] 数据库操作封装
- [ ] API接口开发

### 7.3 第三阶段 (第5-6周)
- [ ] 前端页面开发
- [ ] 结果可视化功能
- [ ] 文件管理功能
- [ ] 系统集成测试

### 7.4 第四阶段 (第7-8周)
- [ ] 性能优化
- [ ] 安全性加固
- [ ] 用户体验优化
- [ ] 部署和上线

## 8. 技术难点和解决方案

### 8.1 大文件上传处理
**问题**: 视频文件较大，上传时间长
**解决方案**: 
- 使用分片上传
- 添加上传进度显示
- 实现断点续传功能

### 8.2 并发检测处理
**问题**: 多用户同时检测造成资源竞争
**解决方案**:
- 使用异步任务队列 (Celery + Redis)
- 限制并发检测数量
- 实现任务优先级管理

### 8.3 模型加载优化
**问题**: YOLOv8模型加载时间长
**解决方案**:
- 应用启动时预加载模型
- 使用模型缓存机制
- 支持多模型版本切换

### 8.4 结果存储优化
**问题**: 检测结果图片占用存储空间大
**解决方案**:
- 定期清理过期文件
- 压缩存储检测结果
- 实现云存储集成

## 9. 性能要求

### 9.1 检测性能
- **图片检测**: ≤ 3秒 (单张1080p图片)
- **视频检测**: ≤ 1分钟 (30秒1080p视频)
- **并发处理**: 支持5个并发检测任务
- **准确率**: COCO数据集mAP ≥ 0.4

### 9.2 系统性能
- **响应时间**: API响应时间 ≤ 500ms
- **并发用户**: 支持100个并发用户
- **存储容量**: 支持10GB文件存储
- **可用性**: 系统可用性 ≥ 99%

## 10. 安全要求

### 10.1 文件安全
- 文件类型验证
- 文件大小限制
- 恶意文件检测
- 文件访问权限控制

### 10.2 数据安全
- 数据库连接加密
- 敏感信息脱敏
- 定期数据备份
- 用户数据隔离

## 11. 部署方案

### 11.1 开发环境
```bash
# Python 3.8+
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 11.2 生产环境
```bash
# Docker部署
docker-compose up -d

# 或Nginx + Gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker
```

### 11.3 配置文件示例
```python
# config.py
import os

class Config:
    # 数据库配置
    MYSQL_HOST = os.getenv('MYSQL_HOST', 'localhost')
    MYSQL_PORT = int(os.getenv('MYSQL_PORT', 3306))
    MYSQL_USER = os.getenv('MYSQL_USER', 'root')
    MYSQL_PASSWORD = os.getenv('MYSQL_PASSWORD', 'password')
    MYSQL_DATABASE = os.getenv('MYSQL_DATABASE', 'detection_db')
    
    # 文件配置
    UPLOAD_PATH = os.getenv('UPLOAD_PATH', './static/uploads')
    MAX_FILE_SIZE = int(os.getenv('MAX_FILE_SIZE', 104857600))  # 100MB
    
    # YOLOv8配置
    MODEL_PATH = os.getenv('MODEL_PATH', './weights/yolov8n.pt')
    CONFIDENCE_THRESHOLD = float(os.getenv('CONFIDENCE_THRESHOLD', 0.5))
    NMS_THRESHOLD = float(os.getenv('NMS_THRESHOLD', 0.45))
    
    # 其他配置
    SECRET_KEY = os.getenv('SECRET_KEY', 'your-secret-key')
    DEBUG = os.getenv('DEBUG', 'False').lower() == 'true'
```

## 12. 测试计划

### 12.1 单元测试
- 模型加载测试
- 检测算法测试
- 数据库操作测试
- API接口测试

### 12.2 集成测试
- 文件上传流程测试
- 检测流程测试
- 结果展示测试
- 错误处理测试

### 12.3 性能测试
- 并发用户测试
- 大文件处理测试
- 长时间运行测试
- 资源消耗测试

## 13. 维护和监控

### 13.1 日志管理
- 应用日志记录
- 错误日志监控
- 性能日志分析
- 用户行为日志

### 13.2 监控指标
- 系统资源使用率
- API响应时间
- 检测成功率
- 错误率统计

### 13.3 备份策略
- 数据库定期备份
- 文件存储备份
- 配置文件备份
- 模型权重备份

---

**文档版本**: v1.0  
**创建日期**: 2024-06-24  
**创建人**: JianWei0204  
**最后更新**: 2024-06-24